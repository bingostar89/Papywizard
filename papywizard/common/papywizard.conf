[General]

# Config file version (str)
CONFIG_VERSION =

# Fullscreen flag (bool)
FULLSCREEN = False


[Core]

# Shooting mode ('mosaic', 'preset')
SHOOTING_MODE = mosaic

# Preset template (str)
PRESET_NAME = 4@0, zenith, nadir


[Configuration]

# Head orientation ('up', 'left', 'right', 'down')
SHOOTING_HEAD_ORIENTATION = up

# Camera orientation ('portrait', 'landscape', 'custom')
SHOOTING_CAMERA_ORIENTATION = portrait

# Camera roll for custom camera orientation (°)
SHOOTING_CAMERA_ROLL = 60.

# Stabilization delay (s)
SHOOTING_STABILIZATION_DELAY = 0.5

# Mosaic initial position ('start', 'end')
MOSAIC_START_FROM = start

# First move direction ('yaw', 'pitch')
MOSAIC_INITAL_DIR = yaw

# Do a Cariage Return after end of first move direction (bool)
MOSAIC_CR = False

# Minimum overlap between shots (%)
MOSAIC_OVERLAP = 0.25

# Sensor factor to 35mm equivalent (float)
CAMERA_SENSOR_COEF = 1.6

# Ratio of the sensor ('3:2', '4:3', '5:4', '16:9')
CAMERA_SENSOR_RATIO = 3:2

# Resolution of the sensor (Mpx)
CAMERA_SENSOR_RESOLUTION = 8.2

# Lens type ('rectilinear', 'fisheye')
LENS_TYPE = rectilinear

# Focal (mm)
LENS_FOCAL = 17.

# Multiplier
LENS_OPTICAL_MULTIPLIER = 1.0

# Shooting area color scheme ('default', 'dark')
SHOOTING_AREA_COLOR_SCHEME = default

# Data storage dir (str)
DATA_STORAGE_DIR =

# File format (str)
DATA_FILE_FORMAT = %(date_time)s_%(mode)s

# Flag to write data file or not (bool)
DATA_FILE_ENABLE = False

# Data title (str)
DATA_TITLE =

# Data GPS (str)
DATA_GPS =

# Data comment (str)
DATA_COMMENT =

# Timer after hour (HH:MM:SS)
TIMER_AFTER = 00:00:00

# Timer every enable (bool)
TIMER_AFTER_ENABLE = False

# Timer every repeat (int)
TIMER_REPEAT = 1

# Timer every enable (bool)
TIMER_REPEAT_ENABLE = False

# Timer every hout (HH:MM:SS)
TIMER_EVERY = 00:00:00

# Logger level, in ('trace', 'debug', 'info', 'warning', 'error', 'exception', 'critical')
LOGGER_LEVEL = debug


[Plugins]

# yaw axis plugin
PLUGIN_YAW_AXIS = Simulation

# pitch axis plugin
PLUGIN_PITCH_AXIS = Simulation

# shutter plugin
PLUGIN_SHUTTER = Simulation

# Bluetooth-to-serial converter address
HARDWARE_BLUETOOTH_DEVICE_ADDRESS = 00:50:C2:58:55:B9

# Serial port
# Can be an int (0 is first port, 1 is second..),
# or a string (/dev/ttyS0, COM1...)
HARDWARE_SERIAL_PORT = 0

# Socket host/port (str/int)
HARDWARE_ETHERNET_HOST = localhost
HARDWARE_ETHERNET_PORT = 7165


[Simulation_yawAxis]

# High limit (float)
HIGH_LIMIT = 360

# High limit (float)
LOW_LIMIT = -360

# axis speed (int)
SPEED = 30

# Driver type ('bluetooth', 'serial', 'ethernet')
DRIVER_TYPE = bluetooth


[Simulation_pitchAxis]

# High limit (float)
HIGH_LIMIT = 360

# High limit (float)
LOW_LIMIT = -360

# axis speed (int)
SPEED = 30


[Simulation_shutter]

# Time value (float)
TIME_VALUE = 0.5

# Mirror lockup flag (bool)
MIRROR_LOCKUP = false

# Bracketing number of pictures (int)
BRACKETING_NB_PICTS = 1

# Bracketing intent ('exposure', 'focus', 'white balance', 'movement')
BRACKETING_INTENT = exposure


[Merlin-Orion_yawAxis]

# Driver type ('bluetooth', 'serial', 'ethernet')
DRIVER_TYPE = bluetooth

# High limit (float)
HIGH_LIMIT = 360

# High limit (float)
LOW_LIMIT = -360

# Driver type ('bluetooth', 'serial', 'ethernet')
DRIVER_TYPE = bluetooth

# Alternate drive method flag (bool)
ALTERNATE_DRIVE = true

# Inertia angle for alternate drive method (float)
INERTIA_ANGLE = 1


[Merlin-Orion_pitchAxis]

# Driver type ('bluetooth', 'serial', 'ethernet')
DRIVER_TYPE = bluetooth

# High limit (float)
HIGH_LIMIT = 360

# High limit (float)
LOW_LIMIT = -360

# Alternate drive method flag (bool)
ALTERNATE_DRIVE = true

# Inertia angle for alternate drive method (float)
INERTIA_ANGLE = 1


[Merlin-Orion_shutter]

# Driver type ('bluetooth', 'serial', 'ethernet')
DRIVER_TYPE = bluetooth

# Time value (float)
TIME_VALUE = 0.5

# Mirror lockup flag (bool)
MIRROR_LOCKUP = false

# Bracketing number of pictures (int)
BRACKETING_NB_PICTS = 1

# Bracketing intent ('exposure', 'focus', 'white balance', 'movement')
BRACKETING_INTENT = exposure

# Width of the high level pulse (int)
PULSE_WIDTH_HIGH = 200

# Width of the low level pulse (int)
PULSE_WIDTH_LOW = 200


[Pololu%20Servo_yawAxis]

# Driver type ('bluetooth', 'serial', 'ethernet')
DRIVER_TYPE = bluetooth

# High limit (float)
HIGH_LIMIT = 360

# High limit (float)
LOW_LIMIT = -360

# axis speed (int)
SPEED = 30

# Servo channel (int)
CHANNEL = 0

# Servo direction ('forward', 'reverse')
DIRECTION = forward

# Servo move for 1ms signel (float)
ANGLE_1MS = 120.

# Servo controller value for neutral position (int)
NEUTRAL_POSITION = 3000


[Pololu%20Servo_pitchAxis]

# Driver type ('bluetooth', 'serial', 'ethernet')
DRIVER_TYPE = bluetooth

# High limit (float)
HIGH_LIMIT = 360

# High limit (float)
LOW_LIMIT = -360

# axis speed (int)
SPEED = 30

# Servo channel (int)
CHANNEL = 1

# Servo direction ('forward', 'reverse')
DIRECTION = forward

# Servo move for 1ms signel (float)
ANGLE_1MS = 120.

# Servo controller value for neutral position (int)
NEUTRAL_POSITION = 3000


[Pololu%20Servo_shutter]

# Driver type ('bluetooth', 'serial', 'ethernet')
DRIVER_TYPE = bluetooth

# Time value (float)
TIME_VALUE = 0.5

# Mirror lockup flag (bool)
MIRROR_LOCKUP = false

# Bracketing number of pictures (int)
BRACKETING_NB_PICTS = 1

# Bracketing intent ('exposure', 'focus', 'white balance', 'movement')
BRACKETING_INTENT = exposure

# Width of the high level pulse (int)
PULSE_WIDTH_HIGH = 200

# Width of the low level pulse (int)
PULSE_WIDTH_LOW = 200

# Servo channel (int)
CHANNEL = 2

# Servo controller value for off (int)
VALUE_OFF = 0

# Servo controller value for on (in)
VALUE_ON = 127


[Generic%20Tethered_shutter]

# Mirror lockup flag (bool)
MIRROR_LOCKUP = false

# Mirror lockup command (str)
MIRROR_LOCKUP_COMMAND = gphoto2 --capture-image

# Shooting lockup command (str)
SHOOT_COMMAND = gphoto2 --capture-image

# Bracketing number of pictures (int)
BRACKETING_NB_PICTS = 1

# Bracketing intent ('exposure', 'focus', 'white balance', 'movement')
BRACKETING_INTENT = exposure


[Timelord_shutter]

# TimeLord program path (str)
PROGRAM_PATH = C:\\Program Files\\OxfordEye\\Timelord\\Timelord.exe

# TimeLord configuration file path (str)
LRD_FILE_PATH = C:\\Documents and Settings\\win2k\\My Documents\\timelord.lrd


[EOS%20Utility_shutter]

# Program path (str)
PROGRAM_PATH = C:\\Program Files\\Papywizard\\EOSBracket.exe

# EOS Utility type ('new', 'old')
EOSUTILITY_TYPE = new

# Exposure bracketing stops (in)
EXPOSURE_BRACKETING_STOPS = 1

# Exposure bracketing type ('0-+', '0+-', '-0+')
EXPOSURE_BRACKETING_TYPE = 0-+

# Exposure bracketing nb picts (int)
EXPOSURE_BRACKETING_NBPICTS = 1

# Shoot pictures flag (bool)
SHOOT_PICTURES = false

# Bulb mode flag (bool)
BULB_MODE = false

# Bulb exposure value (int)
BASE_BULB_EXPOSURE = 1

# Focus mode flag (bool)
FOCUS_MODE = false

# Focus direction ('far', 'near')
FOCUS_DIRECTION = far

# Focus steps ('small', 'medium', 'large')
FOCUS_STEP = medium

# Focus steps count (int)
FOCUS_STEP_COUNT = 1

# Focus bracketing number of pictures (int)
FOCUS_BRACKETING_NBPICTS = 1


[Ursa%20Minor%20USB_shutter]

# Driver type ('bluetooth', 'serial', 'ethernet')
DRIVER_TYPE = serial

# Time value (float)
TIME_VALUE = 0.5

# Mirror lockup flag (bool)
MIRROR_LOCKUP = false

# Bracketing number of pictures (int)
BRACKETING_NB_PICTS = 1

# Bracketing intent ('exposure', 'focus', 'white balance', 'movement')
BRACKETING_INTENT = exposure

# Width of the high level pulse (int)
PULSE_WIDTH_HIGH = 200

# Width of the low level pulse (int)
PULSE_WIDTH_LOW = 200

# Line to use to trigger the shutter
TRIGGER_LINE = RTS

# Flag to invert the signal
TRIGGER_LINE_INVERTED = False


[Ursa%20Minor%20BT2_shutter]

# Driver type ('bluetooth', 'serial', 'ethernet')
DRIVER_TYPE = bluetooth

# Time value (float)
TIME_VALUE = 0.5

# Mirror lockup flag (bool)
MIRROR_LOCKUP = false

# Bracketing number of pictures (int)
BRACKETING_NB_PICTS = 1

# Bracketing intent ('exposure', 'focus', 'white balance', 'movement')
BRACKETING_INTENT = exposure

# Width of the high level pulse (int)
PULSE_WIDTH_HIGH = 200

# Width of the low level pulse (int)
PULSE_WIDTH_LOW = 200


[Gphoto_shutter]

# Mirror lockup flag (bool)
MIRROR_LOCKUP = false

# Bracketing number of pictures (int)
BRACKETING_NB_PICTS = 1

# Step for bracketing
BRACKETING_STEP = 1.

# Bracketing intent ('exposure', 'focus', 'white balance', 'movement')
BRACKETING_INTENT = exposure
